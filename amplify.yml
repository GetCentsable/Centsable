version: 1
frontend:
    phases:
        pre_build:
            commands:
                - 'cd app/'
        build:
            commands:
                - 'npm run build'
    artifacts:
        baseDirectory: /dist
        files:
            - '**/*'
    cache:
        paths: []
